@model OnlineShop.MMA.Areas.Admin.Models.DiscountBuyer.UpdateModel
@{
    ViewData["Title"] = "Buyer with his discount Update";
}

<main class="content">
    <div class="container-fluid p-0">
        <h1 class="h3 mb-3">Buyer with his discount Update</h1>
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <form asp-controller="discountBuyer" asp-action="Update" method="post" class="form-horizontal" enctype="multipart/form-data">
                        @Html.AntiForgeryToken()
                        <input asp-for="DiscountId" type="hidden" />
                        <input asp-for="BuyerId" type="hidden" />
                        <input asp-for="BuyerUserName" type="hidden" />
                        <div class="text-danger" asp-validation-summary="ModelOnly"></div>
                        <div class="card-header">
                            <h5 class="card-title"></h5>
                            <h5 class="card-subtitle text-muted">The details of the buyer with his discount is as follows</h5>
                        </div>
                        <div class="card-body">
                            <div class="form-row">
                                <div class="form-group col-md-6">
                                    <label asp-for="BuyerUserName">Buyer UserName</label>
                                    <input asp-for="BuyerUserName" type="text" class="form-control" disabled="disabled">
                                </div>
                                <div class="form-group col-md-6">
                                    <label asp-for="IsUsed">Is Used</label>
                                    <div class="custom-control custom-switch">
                                        <input asp-for="IsUsed" type="checkbox" class="custom-control-input" pf-validation-for="IsUsed" pf-validationerror-class="is-invalid" pf-adderrormessage-flag=false>
                                        <label class="custom-control-label" for="IsUsed">Toggle this to switch the status</label>
                                    </div>
                                    <div pf-validation-for="IsUsed" pf-validationerror-class="invalid-feedback" pf-adderrormessage-flag=true></div>
                                </div>
                            </div>
                        </div>
                        <div class="card-footer">
                            <a asp-area="Admin" asp-controller="Discount" asp-action="Update" asp-route-id="@Model.DiscountId" class="btn btn-primary">Back</a>
                            <button type="submit" class="btn btn-primary">
                                Submit
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</main>